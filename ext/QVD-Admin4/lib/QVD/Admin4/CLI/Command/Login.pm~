package QVD::Admin4::CLI::Command::Login;
use base qw( CLI::Framework::Command );
use strict;
use warnings;


sub run 
{
    my ($self, $opts, @args) = @_;
    my $login = $args[0];
    $self->cache->set( login => $login );
}

1;
